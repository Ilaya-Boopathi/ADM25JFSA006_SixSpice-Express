export interface Food {
    // name:string,
    // category:string,
    // price:number,
    // id:string,
    // image:string,
    // foodId:string
    id: string;
    name: string;
    price: number;
    category: string;
    available: boolean;
    image: string;
}
